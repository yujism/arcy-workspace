'use client';
import {useEffect,useState,useCallback} from 'react';
import {SidebarProvider,Sidebar,SidebarContent,SidebarHeader,SidebarFooter,SidebarMenu,SidebarMenuItem,SidebarMenuButton,SidebarInset,SidebarTrigger} from '@/components/ui/sidebar';
import {Dialog,DialogContent,DialogHeader,DialogTitle,DialogDescription} from '@/components/ui/dialog';
import {AlertDialog,AlertDialogContent,AlertDialogHeader,AlertDialogTitle,AlertDialogDescription,AlertDialogFooter,AlertDialogCancel,AlertDialogAction} from '@/components/ui/alert-dialog';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
import {Tabs,TabsList,TabsTrigger} from '@/components/ui/tabs';
import {Checkbox} from '@/components/ui/checkbox';
import {Skeleton} from '@/components/ui/skeleton';
import {Toaster,toast} from 'sonner';
import {Home,MessageSquare,CheckSquare,CalendarDays,BookOpen,Plug,ArrowUpRight,ArrowRight,Sparkles,Plus,Search,ChevronLeft,ChevronRight,FileText,ExternalLink,Clock,Check,RefreshCw,Lock,SlidersHorizontal,Send,Trash2,Pencil,LoaderCircle,FolderOpen,Leaf,ArrowUp,TriangleAlert,Database,CalendarPlus} from 'lucide-react';
import type {RecordItem} from '@/lib/data';
type View='Home'|'Ask Arcy'|'Tasks'|'Calendar'|'Knowledge'|'Connections';
const nav=[['Home',Home],['Ask Arcy',MessageSquare],['Tasks',CheckSquare],['Calendar',CalendarDays],['Knowledge',BookOpen]] as const;
const prompts=['Brief gua hari ini','Apa yang belum gua follow-up?','Cari planning delivery'];
const blank={title:'',body:'',due:'',priority:'medium',start:'',end:'',tag:'Personal'};
const dateID=(d=new Date())=>new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Jakarta'}).format(d);
const niceDate=(v:string,opts:Intl.DateTimeFormatOptions={day:'numeric',month:'short'})=>v?new Date(v.length===10?v+'T12:00:00+07:00':v).toLocaleDateString('id-ID',{timeZone:'Asia/Jakarta',...opts}):'Tanpa tanggal';
const clock=(v:string)=>v?.length===10?'Seharian':new Date(v).toLocaleTimeString('id-ID',{timeZone:'Asia/Jakarta',hour:'2-digit',minute:'2-digit'});
const eventDay=(v:string)=>v?.length===10?v:dateID(new Date(v));
async function api(path:string,body?:any,method='POST'){const r=await fetch(path,body===undefined?{cache:'no-store'}:{method,headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j:any=await r.json();if(!r.ok)throw new Error(j.error||'Permintaan gagal. Coba lagi.');return j;}
export default function Workspace(){
const [view,setView]=useState<View>('Home'),[items,setItems]=useState<RecordItem[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[connected,setConnected]=useState({google:false,ai:false}),[uid,setUid]=useState('');
const [query,setQuery]=useState(''),[question,setQuestion]=useState(''),[asking,setAsking]=useState(false),[taskFilter,setTaskFilter]=useState('open'),[day,setDay]=useState(dateID()),[noteFilter,setNoteFilter]=useState('all');
const [editor,setEditor]=useState<null|'task'|'note'|'event'>(null),[editing,setEditing]=useState<RecordItem|null>(null),[form,setForm]=useState(blank),[saving,setSaving]=useState(false),[detail,setDetail]=useState<RecordItem|null>(null),[remove,setRemove]=useState<RecordItem|null>(null),[publish,setPublish]=useState<RecordItem|null>(null);
const [setup,setSetup]=useState<'google'|'ai'|null>(null),[importer,setImporter]=useState(false),[driveQuery,setDriveQuery]=useState(''),[driveFiles,setDriveFiles]=useState<any[]>([]),[sheetId,setSheetId]=useState(''),[range,setRange]=useState(''),[busy,setBusy]=useState(''),[calendarCursor,setCalendarCursor]=useState<string|null>(null);
const load=useCallback(async()=>{try{const d=await api('/api/workspace');setItems(d.items);setConnected(d.connections);setUid(d.user.id);setError('');}catch(e){setError((e as Error).message);}finally{setLoading(false)}},[]);
useEffect(()=>{load();const v=new URLSearchParams(location.search).get('view');if(v&&[...nav.map(n=>n[0]),'Connections'].includes(v as any))setView(v as View);},[load]);
function go(v:View){setView(v);setQuery('');history.replaceState(null,'','?view='+encodeURIComponent(v));}
const tasks=items.filter(x=>x.kind==='task'),notes=items.filter(x=>x.kind==='note'),messages=items.filter(x=>x.kind==='message').sort((a,b)=>a.updated.localeCompare(b.updated));
const events=items.filter(x=>x.kind==='event').sort((a,b)=>String(a.meta.start).localeCompare(String(b.meta.start)));
const openTasks=tasks.filter(x=>x.meta.status!=='done'),today=dateID(),overdue=openTasks.filter(x=>x.meta.due&&x.meta.due<today),todayEvents=events.filter(x=>x.meta.start&&!x.meta.recurrence&&eventDay(x.meta.start)===today);
const search=(x:RecordItem)=>!query||[x.title,x.body,x.meta.tag].join(' ').toLowerCase().includes(query.toLowerCase());
function openEditor(kind:'task'|'note'|'event',r?:RecordItem){setEditing(r||null);const local=(d:string)=>d?new Date(new Date(d).getTime()+7*3600000).toISOString().slice(0,16):'';setForm(r?{title:r.title,body:r.body,due:r.meta.due||'',priority:r.meta.priority||'medium',start:local(r.meta.start),end:local(r.meta.end),tag:r.meta.tag||'Personal'}:{...blank,start:day+'T09:00',end:day+'T10:00'});setEditor(kind);}
async function save(e:React.FormEvent){e.preventDefault();setSaving(true);try{const meta=editor==='task'?{status:editing?.meta.status||'open',priority:form.priority,due:form.due,...(editing?.meta.source?{source:editing.meta.source}:{})}:editor==='event'?{start:new Date(form.start+':00+07:00').toISOString(),end:new Date(form.end+':00+07:00').toISOString(),source:'Arcy Calendar'}:{tag:form.tag,source:'Catatan pribadi'};await api('/api/workspace',{id:editing?.id,kind:editor,title:form.title,body:form.body,meta});await load();setEditor(null);toast.success(editing?'Perubahan disimpan':'Berhasil ditambahkan');}catch(e){toast.error((e as Error).message);}finally{setSaving(false)}}
async function toggle(r:RecordItem){try{await api('/api/workspace',{...r,meta:{...r.meta,status:r.meta.status==='done'?'open':'done'}});await load();}catch(e){toast.error((e as Error).message)}}
async function ask(q=question){if(!q.trim()||asking)return;setQuestion(q);go('Ask Arcy');setAsking(true);try{await api('/api/ask',{question:q});setQuestion('');await load();}catch(e){toast.error((e as Error).message);}finally{setAsking(false)}}
async function google(body:any){setBusy(body.action);try{const r=await api('/api/google',body);if(body.action==='browse')setDriveFiles(r.files||[]);else{await load();if(body.action==='calendar'){setCalendarCursor(r.nextPageToken);toast.success(r.complete?`Calendar diperbarui: ${r.changed} perubahan`:'Sebagian agenda diimpor. Klik Lanjutkan sync.');}else toast.success('Berhasil disimpan');}return r;}catch(e){toast.error((e as Error).message);return null;}finally{setBusy('')}}
function captureSource(r:RecordItem){openEditor('task');setForm(f=>({...f,title:'Review '+r.title,body:'Sumber: '+r.title+(r.meta.url?'\n'+r.meta.url:'')}));setDetail(null);}
const taskRow=(r:RecordItem)=><div className="task-row" key={r.id}><Checkbox aria-label={'Selesaikan '+r.title} checked={r.meta.status==='done'} onCheckedChange={()=>toggle(r)}/><button className={'task-main '+(r.meta.status==='done'?'completed':'')} onClick={()=>openEditor('task',r)}><strong>{r.title}</strong><span>{r.meta.due?niceDate(r.meta.due):'Tanpa deadline'}{r.body?' · '+r.body.slice(0,65):''}</span></button><span className={'priority '+r.meta.priority}>{r.meta.priority==='high'?'High':r.meta.priority==='low'?'Low':'Medium'}</span><button className="icon-button row-delete" aria-label={'Hapus '+r.title} onClick={()=>setRemove(r)}><Trash2 size={15}/></button></div>;
const composer=<div className="composer"><textarea aria-label="Pertanyaan untuk Arcy" value={question} onChange={e=>setQuestion(e.target.value)} placeholder="Tanya tentang pekerjaan lu, atau cari sesuatu…" onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();ask();}}}/><div className="composer-bottom"><span><BookOpen size={14}/>{notes.length} sumber di workspace</span><button className="send-button" aria-label="Kirim pertanyaan" disabled={asking||!question.trim()} onClick={()=>ask()}>{asking?<LoaderCircle className="spin" size={18}/>:<ArrowUp size={19}/>}</button></div></div>;
return <SidebarProvider><Sidebar className="arcy-sidebar"><SidebarHeader><div className="brand"><span className="brand-symbol">a</span>arcy<span className="brand-dot">.</span></div><div className="workspace-switch"><span className="mini-avatar">Y</span><div>Yuji’s workspace<small>Personal</small></div><Lock size={13}/></div></SidebarHeader><SidebarContent><p className="nav-label">WORKSPACE</p><SidebarMenu>{nav.map(([label,Icon])=><SidebarMenuItem key={label}><SidebarMenuButton className="nav-item" isActive={view===label} onClick={()=>go(label)}><Icon/><span>{label}</span>{label==='Tasks'&&openTasks.length>0&&<span className="nav-count">{openTasks.length}</span>}</SidebarMenuButton></SidebarMenuItem>)}</SidebarMenu><div className="sidebar-divider"/><p className="nav-label">YOUR SOURCES</p><button className="source-nav" onClick={()=>go('Connections')}><span className="service-letter drive">D</span>Google Drive<span className={'tiny-state '+(connected.google?'on':'')}/></button><button className="source-nav" onClick={()=>go('Connections')}><span className="service-letter cal">C</span>Google Calendar<span className={'tiny-state '+(connected.google?'on':'')}/></button><button className="source-nav" onClick={()=>go('Connections')}><span className="service-letter sheets">S</span>Google Sheets<span className={'tiny-state '+(connected.google?'on':'')}/></button><div className="sidebar-note"><Leaf size={20}/><p>A little less scattered.<br/>A little more focused.</p><small>Your own space to think.</small></div></SidebarContent><SidebarFooter><SidebarMenuButton className="nav-item" isActive={view==='Connections'} onClick={()=>go('Connections')}><SlidersHorizontal/><span>Connections & settings</span></SidebarMenuButton><div className="profile"><span className="avatar">Y</span><div>Yuji<small>Personal workspace</small></div><Lock size={14}/></div></SidebarFooter></Sidebar><SidebarInset className="workspace"><header className="topbar"><div className="breadcrumb"><SidebarTrigger/><span>Workspace</span><span className="slash">/</span><strong>{view}</strong></div><div className="top-right"><span className="private-label"><Lock size={12}/>Only you</span><span className="top-avatar">Y</span></div></header><main className="main">
{error&&<div className="error-banner" role="alert"><TriangleAlert size={18}/><span>{error}</span><button onClick={load}>Coba lagi</button></div>}
{view==='Home'&&<><div className="page-heading"><div><p className="eyebrow">{new Date().toLocaleDateString('en-GB',{timeZone:'Asia/Jakarta',weekday:'long',day:'numeric',month:'long'})} <span>·</span> JAKARTA</p><h1>Your day, a little clearer.</h1><p>Selamat datang, Yuji. Mulai dari yang paling penting.</p></div><button className="primary" onClick={()=>openEditor('task')}><Plus size={17}/>New task</button></div><div className="home-grid"><div className="home-primary"><section className="ask-card"><div className="ask-heading"><span className="spark-box"><Sparkles size={20}/></span><span>YOUR THINKING PARTNER</span><span className="subtle-badge">{connected.ai?'AI connected':'Search mode'}</span></div><h2>What’s on your mind?</h2><p>Temukan konteks. Tentukan langkah berikutnya.</p>{composer}<div className="prompt-chips">{prompts.map(p=><button key={p} onClick={()=>ask(p)}>{p}<ArrowUpRight size={13}/></button>)}</div></section><div className="stats"><button onClick={()=>go('Tasks')}><span><CheckSquare size={17}/>Open tasks</span><strong>{loading?'—':openTasks.length}<small>{overdue.length?overdue.length+' lewat deadline':'Ready when you are'}</small></strong></button><button onClick={()=>go('Calendar')}><span><CalendarDays size={17}/>Today’s agenda</span><strong>{loading?'—':todayEvents.length}<small>{connected.google?'Termasuk Calendar tersinkron':'Agenda lokal'}</small></strong></button><button onClick={()=>go('Knowledge')}><span><BookOpen size={17}/>Knowledge</span><strong>{loading?'—':notes.length}<small>Sumber tersimpan</small></strong></button></div><section className="panel focus-panel"><div className="section-heading"><h2>Focus list <span className="count">{openTasks.length}</span></h2><button className="text-button" onClick={()=>go('Tasks')}>All tasks <ArrowRight size={14}/></button></div>{loading?<Skeleton className="h-28 w-full"/>:openTasks.length?openTasks.slice(0,4).map(taskRow):<div className="empty-focus"><div className="empty-icon"><CheckSquare size={26}/></div><h3>One thing at a time.</h3><p>Tambahkan prioritas pertama lu untuk hari ini.</p><button className="text-button green" onClick={()=>openEditor('task')}><Plus size={16}/>Add your first task</button></div>}<button className="add-row" onClick={()=>openEditor('task')}><Plus size={16}/>Capture a task</button></section><section className="recent-section"><div className="section-heading"><h2>At your fingertips</h2><button className="text-button" onClick={()=>go('Knowledge')}>Open knowledge <ArrowRight size={14}/></button></div><div className="knowledge-preview">{notes.length?notes.slice(0,2).map(n=><button key={n.id} className="note-preview" onClick={()=>setDetail(n)}><FileText/><span><strong>{n.title}</strong><small>{n.meta.source||'Catatan'} · {niceDate(n.updated)}</small></span><ArrowUpRight size={16}/></button>):<><button className="note-preview" onClick={()=>openEditor('note')}><FileText/><span><strong>Give your ideas a home</strong><small>Simpan catatan dan konteks pekerjaan.</small></span><Plus size={17}/></button><button className="note-preview" onClick={()=>go('Connections')}><FolderOpen/><span><strong>Bring your work together</strong><small>Hubungkan sumber Google lu.</small></span><ArrowUpRight size={16}/></button></>}</div></section></div><aside className="day-column"><section className="panel agenda-panel"><div className="section-heading"><h2>Today’s schedule</h2><button className="icon-button" aria-label="Tambah agenda" onClick={()=>openEditor('event')}><Plus size={18}/></button></div><div className="agenda-date"><span>{new Date().toLocaleDateString('en-GB',{timeZone:'Asia/Jakarta',weekday:'long',day:'numeric',month:'short'})}</span><span>WIB</span></div><div className="mini-week">{Array.from({length:7},(_,i)=>{const d=new Date(today+'T12:00:00+07:00');d.setDate(d.getDate()-((d.getDay()+6)%7)+i);const id=dateID(d);return <button className={id===today?'selected':''} key={id} onClick={()=>{setDay(id);go('Calendar')}}><small>{['M','T','W','T','F','S','S'][i]}</small><span>{d.getDate()}</span></button>})}</div>{todayEvents.length?<div className="agenda-items">{todayEvents.slice(0,4).map(e=><button className="agenda-item" key={e.id} onClick={()=>setDetail(e)}><small>{clock(e.meta.start)}</small><strong>{e.title}</strong><span>{e.meta.source||'Arcy Calendar'}</span></button>)}</div>:<div className="schedule-empty"><CalendarDays size={29}/><h3>A little breathing room.</h3><p>Belum ada agenda tersimpan hari ini.</p><button className="text-button green" onClick={()=>openEditor('event')}>Plan some time <ArrowRight size={14}/></button></div>}<button className="agenda-footer" onClick={()=>go('Calendar')}>View calendar <ArrowUpRight size={15}/></button></section><section className="connection-callout"><div className="callout-icon"><Plug size={19}/></div><h3>{connected.google?'Your sources, together.':'Make it your workspace.'}</h3><p>{connected.google?'Impor dokumen, baca range Sheets, dan sinkronkan agenda saat dibutuhkan.':'Hubungkan Google untuk membawa dokumen dan agenda ke satu tempat.'}</p><button onClick={()=>go('Connections')}>{connected.google?'Manage sources':'Set up connections'}<ArrowRight size={15}/></button></section><div className="quiet-note"><Lock size={13}/><span>Private by default.<br/>Lu yang menentukan langkah berikutnya.</span></div></aside></div></>}
{view==='Tasks'&&<><PageHeading eyebrow="ONE STEP AT A TIME" title="Your tasks" description="Capture it. Prioritize it. Get it off your mind." action={<button className="primary" onClick={()=>openEditor('task')}><Plus size={17}/>New task</button>}/><div className="toolbar"><Tabs value={taskFilter} onValueChange={setTaskFilter}><TabsList><TabsTrigger value="open">Open · {openTasks.length}</TabsTrigger><TabsTrigger value="done">Completed · {tasks.length-openTasks.length}</TabsTrigger><TabsTrigger value="all">All</TabsTrigger></TabsList></Tabs><SearchBox value={query} onChange={setQuery}/></div><section className="panel">{tasks.filter(x=>(taskFilter==='all'||(taskFilter==='done'?x.meta.status==='done':x.meta.status!=='done'))&&search(x)).sort((a,b)=>(a.meta.due||'9999').localeCompare(b.meta.due||'9999')).map(taskRow)}{!tasks.filter(x=>(taskFilter==='all'||(taskFilter==='done'?x.meta.status==='done':x.meta.status!=='done'))&&search(x)).length&&<Empty icon={<CheckSquare/>} title="Nothing on this list yet." text="Task baru akan tersimpan di workspace lu."/>}<button className="add-row" onClick={()=>openEditor('task')}><Plus size={16}/>Add a task</button></section></>}
{view==='Knowledge'&&<><PageHeading eyebrow="LESS SEARCHING, MORE KNOWING" title="Your knowledge" description="Catatan, konteks, dan sumber pekerjaan lu." action={<div className="button-group"><button className="secondary" onClick={()=>connected.google?setImporter(true):setSetup('google')}><FolderOpen size={16}/>Import Google</button><button className="primary" onClick={()=>openEditor('note')}><Plus size={17}/>New note</button></div>}/><div className="toolbar"><Tabs value={noteFilter} onValueChange={setNoteFilter}><TabsList><TabsTrigger value="all">All sources</TabsTrigger><TabsTrigger value="local">My notes</TabsTrigger><TabsTrigger value="google">Google</TabsTrigger></TabsList></Tabs><SearchBox value={query} onChange={setQuery}/></div><div className="note-grid">{notes.filter(n=>search(n)&&(noteFilter==='all'||(noteFilter==='google'?n.meta.provider==='google':n.meta.provider!=='google'))).map(n=><button className="note-card" key={n.id} onClick={()=>setDetail(n)}><div className="note-card-top"><span className="file-icon"><FileText size={20}/></span><span className="tag">{n.meta.tag||'Note'}</span></div><h3>{n.title}</h3><p>{n.body.slice(0,170)||'Tanpa isi.'}</p><footer><span>{n.meta.source||'Catatan'}</span><span>{niceDate(n.updated)}</span></footer></button>)}</div>{!notes.filter(n=>search(n)&&(noteFilter==='all'||(noteFilter==='google'?n.meta.provider==='google':n.meta.provider!=='google'))).length&&<section className="panel"><Empty icon={<BookOpen/>} title="A home for everything worth keeping." text="Tambah catatan atau impor sumber Google. Isinya bisa langsung dicari di Ask Arcy."/><button className="empty-action primary" onClick={()=>openEditor('note')}><Plus size={16}/>Create a note</button></section>}</>}
{view==='Calendar'&&<><PageHeading eyebrow="MAKE SPACE FOR YOUR WORK" title="Your calendar" description="Agenda dalam waktu Jakarta (WIB)." action={<div className="button-group"><button className="secondary" disabled={!!busy} onClick={()=>connected.google?google({action:'calendar',pageToken:calendarCursor}):setSetup('google')}><RefreshCw size={16} className={busy?'spin':''}/>{calendarCursor?'Lanjutkan sync':'Sync Google'}</button><button className="primary" onClick={()=>openEditor('event')}><Plus size={17}/>New event</button></div>}/><div className="toolbar"><div className="button-group"><button className="icon-button outlined" aria-label="Hari sebelumnya" onClick={()=>{const d=new Date(day+'T12:00:00+07:00');d.setDate(d.getDate()-1);setDay(dateID(d))}}><ChevronLeft size={18}/></button><input aria-label="Tanggal agenda" type="date" value={day} onChange={e=>setDay(e.target.value||today)} className="date-picker"/><button className="icon-button outlined" aria-label="Hari berikutnya" onClick={()=>{const d=new Date(day+'T12:00:00+07:00');d.setDate(d.getDate()+1);setDay(dateID(d))}}><ChevronRight size={18}/></button><button className="text-button" onClick={()=>setDay(today)}>Today</button></div><span className="muted">{niceDate(day,{weekday:'long',day:'numeric',month:'long'})}</span></div><section className="panel calendar-list">{events.filter(e=>e.meta.start&&!e.meta.recurrence&&eventDay(e.meta.start)===day).map(e=><button className="calendar-event" key={e.id} onClick={()=>setDetail(e)}><span className="event-time">{clock(e.meta.start)}<small>{clock(e.meta.end)}</small></span><span className="event-info"><strong>{e.title}</strong><span>{e.body||e.meta.source||'Agenda lokal'}</span></span><span className="tag">{e.meta.provider==='google'?'Google':'Local'}</span><ArrowUpRight size={16}/></button>)}{!events.some(e=>e.meta.start&&!e.meta.recurrence&&eventDay(e.meta.start)===day)&&<Empty icon={<CalendarDays/>} title="Space to plan something good." text="Belum ada agenda tersimpan pada tanggal ini."/>}<button className="add-row" onClick={()=>openEditor('event')}><Plus size={16}/>Schedule something</button></section>{events.some(e=>e.meta.recurrence)&&<section className="panel mt-panel"><h2>Recurring series</h2><p className="muted">Rincian kejadian berulang dibuka langsung di Google Calendar.</p>{events.filter(e=>e.meta.recurrence).map(e=><a className="recurring-link" href={e.meta.url} target="_blank" rel="noreferrer" key={e.id}>{e.title}<ExternalLink size={15}/></a>)}</section>}</>}
{view==='Ask Arcy'&&<div className="chat-layout"><PageHeading eyebrow="A LITTLE CONTEXT GOES A LONG WAY" title="Ask Arcy" description={connected.ai?'Jawaban AI berdasarkan sumber di workspace, dengan kutipan.':'Pencarian sumber aktif. Hubungkan model AI untuk jawaban generatif.'}/><div className="chat-messages">{!messages.length?<div className="chat-welcome"><span className="large-spark"><Sparkles size={30}/></span><h2>Let’s connect the dots.</h2><p>Tanya tentang catatan, task, atau agenda yang sudah tersimpan.</p><div className="welcome-prompts">{prompts.map(p=><button key={p} onClick={()=>ask(p)}>{p}<ArrowUpRight size={17}/></button>)}</div></div>:messages.map(m=><article className="conversation" key={m.id}><div className="question"><span className="top-avatar">Y</span><h3>{m.title}</h3></div><div className="answer"><span className="answer-logo"><Sparkles size={17}/></span><div><div className="answer-label">Arcy <span>{m.meta.mode==='ai'?'Grounded AI':'Source search'}</span></div><p>{m.body}</p>{m.meta.sources?.length>0&&<div className="citation-list">{m.meta.sources.map((s:any)=><button key={s.id} onClick={()=>{const r=items.find(x=>x.id===s.id);if(r)setDetail(r);else toast('Sumber sudah tidak ada di workspace.')}}><span>{s.number}</span>{s.title}<ArrowUpRight size={12}/></button>)}</div>}<div className="answer-actions"><button onClick={()=>{openEditor('task');setForm(f=>({...f,title:m.title.slice(0,250),body:m.body}));}}><Plus size={14}/>Create task</button><small>{niceDate(m.updated)}</small></div></div></div></article>)}{asking&&<div className="answer-pending" role="status"><LoaderCircle className="spin" size={17}/>Mencari konteks di workspace…</div>}</div><div className="chat-compose">{composer}<small>{connected.ai?'Periksa sumber sebelum mengambil keputusan.':'Mode pencarian teks · Belum menggunakan model AI'}</small></div></div>}
{view==='Connections'&&<><PageHeading eyebrow="YOUR WORK, CONNECTED" title="Connections" description="Pilih sumber yang memberi konteks pada workspace lu."/><div className="connection-grid">{[{name:'Google Drive',letter:'D',cls:'drive',desc:'Cari dan impor Google Docs pilihan lu sebagai snapshot yang bisa ditanya.',action:'Browse documents',click:()=>setImporter(true)},{name:'Google Sheets',letter:'S',cls:'sheets',desc:'Baca range spreadsheet langsung. Simpan hasilnya beserta sumber dan waktu impor.',action:'Import a range',click:()=>setImporter(true)},{name:'Google Calendar',letter:'C',cls:'cal',desc:'Sinkronisasi agenda utama. Kirim agenda lokal setelah lu meninjau detailnya.',action:'Sync calendar',click:()=>google({action:'calendar',pageToken:calendarCursor})}].map(c=><section className="connection-card" key={c.name}><div className="connection-card-top"><span className={'service-tile '+c.cls}>{c.letter}</span><span className={'connection-status '+(connected.google?'ready':'')}>{connected.google?'Configured':'Not connected'}</span></div><h2>{c.name}</h2><p>{c.desc}</p><button className={connected.google?'secondary':'primary'} disabled={!!busy} onClick={()=>connected.google?c.click():setSetup('google')}>{connected.google?c.action:'Set up connection'}<ArrowUpRight size={15}/></button></section>)}</div><section className="panel model-panel"><span className="large-spark"><Sparkles size={25}/></span><div><h2>Arcy intelligence</h2><p>{connected.ai?'Model AI terkonfigurasi. Jawaban memakai sumber yang ditemukan di workspace.':'Search sudah bisa dipakai. Aktifkan model untuk rangkuman dan jawaban dengan kutipan.'}</p></div><span className={'connection-status '+(connected.ai?'ready':'')}>{connected.ai?'Configured':'Not connected'}</span><button className="secondary" onClick={()=>setSetup('ai')}>Configure <ArrowUpRight size={15}/></button></section><section className="settings-notes"><div><Lock/><h3>Lu tetap pegang kendali.</h3><p>Sheets hanya dibaca. Pengiriman agenda ke Google selalu melalui preview dan konfirmasi.</p></div><div><Database/><h3>A place that remembers.</h3><p>Task, catatan, agenda lokal, dan percakapan disimpan di workspace. Impor Google berupa snapshot saat lu meminta.</p></div></section></>}
<footer className="workspace-footer"><span>arcy workspace</span><span>Less noise. More headspace.</span></footer></main></SidebarInset>
<Dialog open={!!editor} onOpenChange={v=>{if(!saving&&!v)setEditor(null)}}><DialogContent className="arcy-dialog"><DialogHeader><DialogTitle>{editing?'Edit':'New'} {editor==='note'?'note':editor==='event'?'event':'task'}</DialogTitle><DialogDescription>{editor==='event'?'Disimpan sebagai agenda lokal. Lu bisa kirim ke Google setelah meninjau.':'Simpan di workspace pribadi lu.'}</DialogDescription></DialogHeader><form onSubmit={save} className="editor-form"><label>Title<input required maxLength={250} autoFocus value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder={editor==='task'?'Apa yang perlu dikerjakan?':'Beri judul yang mudah ditemukan'}/></label><label>{editor==='note'?'Content':'Notes'}<textarea rows={editor==='note'?9:4} value={form.body} maxLength={100000} onChange={e=>setForm({...form,body:e.target.value})} placeholder="Tambahkan konteks…"/></label>{editor==='task'&&<div className="form-two"><label>Due date<input type="date" value={form.due} onChange={e=>setForm({...form,due:e.target.value})}/></label><label>Priority<Select value={form.priority} onValueChange={v=>setForm({...form,priority:v})}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="high">High</SelectItem><SelectItem value="medium">Medium</SelectItem><SelectItem value="low">Low</SelectItem></SelectContent></Select></label></div>}{editor==='event'&&<div className="form-two"><label>Starts · WIB<input required type="datetime-local" value={form.start} onChange={e=>setForm({...form,start:e.target.value})}/></label><label>Ends · WIB<input required type="datetime-local" value={form.end} onChange={e=>setForm({...form,end:e.target.value})}/></label></div>}{editor==='note'&&<label>Tag<input value={form.tag} maxLength={40} onChange={e=>setForm({...form,tag:e.target.value})}/></label>}<div className="dialog-actions"><button type="button" className="secondary" onClick={()=>setEditor(null)} disabled={saving}>Cancel</button><button className="primary" disabled={saving}>{saving?<LoaderCircle className="spin" size={16}/>:<Check size={16}/>}Save {editor}</button></div></form></DialogContent></Dialog>
<Dialog open={!!detail} onOpenChange={v=>!v&&setDetail(null)}><DialogContent className="arcy-dialog detail-dialog"><DialogHeader><DialogTitle>{detail?.title}</DialogTitle><DialogDescription>{detail?.meta.source||'Workspace'} · Disimpan {detail&&niceDate(detail.updated)}{detail?.meta.provider==='google'?' · Snapshot':''}</DialogDescription></DialogHeader>{detail?.kind==='event'&&<div className="event-detail-time"><CalendarDays size={18}/>{niceDate(detail.meta.start)} · {clock(detail.meta.start)} – {clock(detail.meta.end)} WIB</div>}<div className="note-body">{detail?.body||'Tidak ada catatan tambahan.'}</div><div className="detail-actions">{detail?.meta.url&&<a className="secondary" href={detail.meta.url} target="_blank" rel="noreferrer">Open source<ExternalLink size={14}/></a>}{detail&&<button className="secondary" onClick={()=>captureSource(detail)}><Plus size={15}/>Create task</button>}{detail&&detail.meta.provider!=='google'&&<button className="secondary" onClick={()=>{openEditor(detail.kind as any,detail);setDetail(null)}}><Pencil size={14}/>Edit</button>}{detail?.kind==='event'&&detail.meta.provider!=='google'&&<button className="primary" onClick={()=>connected.google?setPublish(detail):setSetup('google')}><CalendarPlus size={15}/>Send to Google</button>}{detail&&<button className="icon-button" aria-label="Hapus dari workspace" onClick={()=>setRemove(detail)}><Trash2 size={17}/></button>}</div></DialogContent></Dialog>
<AlertDialog open={!!remove} onOpenChange={v=>!v&&setRemove(null)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Hapus dari workspace?</AlertDialogTitle><AlertDialogDescription>“{remove?.title}” akan dihapus dari Arcy. File atau event asli di Google tetap ada.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction onClick={async()=>{try{await api('/api/workspace',{id:remove?.id},'DELETE');setDetail(null);await load();toast.success('Item dihapus');}catch(e){toast.error((e as Error).message)}}}>Delete</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog>
<AlertDialog open={!!publish} onOpenChange={v=>!v&&setPublish(null)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Kirim agenda ke Google Calendar?</AlertDialogTitle><AlertDialogDescription>{publish?.title}<br/>{publish&&niceDate(publish.meta.start)} · {publish&&clock(publish.meta.start)}–{publish&&clock(publish.meta.end)} WIB<br/>Agenda akan dibuat di kalender utama lu, tanpa mengundang peserta.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction onClick={async()=>{const r=await google({action:'publish-event',id:publish?.id});if(r)setDetail(null)}}>Approve & send</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog>
<Dialog open={!!setup} onOpenChange={v=>!v&&setSetup(null)}><DialogContent className="arcy-dialog"><DialogHeader><DialogTitle>{setup==='ai'?'Connect an AI model':'Connect your Google account'}</DialogTitle><DialogDescription>Koneksi belum aktif sampai konfigurasi server selesai dan akses berhasil.</DialogDescription></DialogHeader>{setup==='google'?<div className="setup-content"><p>Koneksi Google di ChatGPT tidak otomatis memberikan akses ke aplikasi ini. Arcy memakai kredensial Google milik lu sendiri.</p><ol><li>Aktifkan Drive, Sheets, dan Calendar API di project Google Cloud.</li><li>Siapkan OAuth client dan refresh token dengan izin baca Drive, Sheets, serta Calendar. Untuk mengirim agenda, tambahkan izin <code>calendar.events</code>.</li><li>Konfigurasikan secret server berikut melalui pengelola aplikasi:</li></ol><pre>GOOGLE_CLIENT_ID{'\n'}GOOGLE_CLIENT_SECRET{'\n'}GOOGLE_REFRESH_TOKEN{'\n'}GOOGLE_OWNER_USER_ID</pre><p>Owner ID aplikasi:</p><code className="owner-id">{uid||'Masuk terlebih dahulu'}</code><a className="text-button green" href="https://developers.google.com/identity/protocols/oauth2/web-server" target="_blank" rel="noreferrer">Panduan resmi Google<ExternalLink size={14}/></a><p className="muted">MVP: impor Google Docs pilihan, range Sheets, dan sync Calendar saat diminta. PDF dan sinkronisasi otomatis belum tersedia.</p></div>:<div className="setup-content"><p>Model memerlukan API key OpenAI yang dikonfigurasi sebagai secret server. Jangan simpan key di catatan atau percakapan.</p><pre>OPENAI_API_KEY{'\n'}OPENAI_MODEL=gpt-4.1-mini</pre><p>Setelah dikonfigurasi, Arcy mengirim pertanyaan dan cuplikan sumber relevan ke provider AI. Tanpa key, pencarian sumber tetap berfungsi.</p></div>}<button className="primary" onClick={async()=>{await load();toast('Status koneksi diperbarui')}}><RefreshCw size={16}/>Refresh connection status</button></DialogContent></Dialog>
<Dialog open={importer} onOpenChange={setImporter}><DialogContent className="arcy-dialog import-dialog"><DialogHeader><DialogTitle>Bring your context in</DialogTitle><DialogDescription>Impor snapshot dari Google Docs atau range Google Sheets.</DialogDescription></DialogHeader><div className="import-section"><h3>Google Docs</h3><form className="inline-form" onSubmit={e=>{e.preventDefault();google({action:'browse',query:driveQuery})}}><input value={driveQuery} onChange={e=>setDriveQuery(e.target.value)} placeholder="Cari judul dokumen…" aria-label="Cari Google Docs"/><button className="secondary" disabled={!!busy}><Search size={16}/>Search</button></form><div className="drive-results">{driveFiles.map(f=><div key={f.id}><FileText size={17}/><span>{f.name}</span><button className="text-button green" disabled={!!busy} onClick={()=>google({action:'import',id:f.id})}>Import<Plus size={14}/></button></div>)}</div><small className="muted">Menampilkan hingga 30 dokumen terbaru yang cocok. Persempit judul untuk hasil lain.</small></div><div className="import-section"><h3>Google Sheets</h3><form className="editor-form" onSubmit={e=>{e.preventDefault();google({action:'sheets',id:sheetId,range})}}><label>Spreadsheet URL or ID<input required value={sheetId} onChange={e=>setSheetId(e.target.value)} placeholder="https://docs.google.com/spreadsheets/d/…"/></label><label>Range<input required value={range} onChange={e=>setRange(e.target.value)} placeholder="'KRA_HO'!A1:Q100"/></label><button className="primary" disabled={!!busy}>{busy?<LoaderCircle className="spin" size={15}/>:<Plus size={15}/>}Read & import range</button></form></div></DialogContent></Dialog><Toaster position="bottom-right" richColors/></SidebarProvider>;
}
function PageHeading({eyebrow,title,description,action}:{eyebrow:string;title:string;description:string;action?:React.ReactNode}){return <div className="page-heading"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{description}</p></div>{action}</div>}
function SearchBox({value,onChange}:{value:string;onChange:(s:string)=>void}){return <label className="search-box"><Search size={16}/><input value={value} onChange={e=>onChange(e.target.value)} placeholder="Search…" aria-label="Cari item"/></label>}
function Empty({icon,title,text}:{icon:React.ReactNode;title:string;text:string}){return <div className="empty-state"><span className="empty-icon">{icon}</span><h3>{title}</h3><p>{text}</p></div>}
