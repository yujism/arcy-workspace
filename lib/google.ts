import {runtime,readyGoogle} from './data';
export async function googleToken(owner:string){if(!readyGoogle(owner))throw new Error('Google belum terhubung. Buka Connections untuk konfigurasi.');const e=runtime();const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({client_id:e.GOOGLE_CLIENT_ID!,client_secret:e.GOOGLE_CLIENT_SECRET!,refresh_token:e.GOOGLE_REFRESH_TOKEN!,grant_type:'refresh_token'}),signal:AbortSignal.timeout(20000)});if(!r.ok)throw new Error('Otorisasi Google gagal. Periksa atau perbarui koneksi.');const t:any=await r.json();return t.access_token as string;}
export async function googleFetch(token:string,url:string,options:RequestInit={}){const r=await fetch(url,{...options,headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json',...options.headers},signal:AbortSignal.timeout(25000)});if(!r.ok){if(r.status===410)throw new Error('SYNC_EXPIRED');throw new Error(`Google menolak permintaan (${r.status}). Periksa izin dan akses sumber.`);}return r;}
