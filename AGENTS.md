# Arcy Workspace

For coding work, read and apply `.agents/skills/ponytail/SKILL.md` in full mode.
For UI work, also read `DESIGN.md` and the relevant guidance in `.agents/skills/design-taste-frontend/SKILL.md`.
Keep all interface text in English. Preserve user-authored and imported content.
Verify application changes with `node node_modules/typescript/bin/tsc --noEmit` and `node scripts/build-pages.mjs`. GitHub Pages serves `docs/`.
